# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DocumentManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DocumentManager
  pod 'GoogleSignIn', '~> 6.0'
  pod 'GoogleAPIClientForREST/Drive', '~> 1.3.7'

end


## Define the platform as macOS for macOS targets
##platform :macos, '10.15'  # Adjust the version based on your requirements
## Define the platform as iOS for iOS targets
##platform :ios, '13.0'  # You can adjust the iOS version to your needs
#
#target 'DocumentManager' do
#  use_frameworks!
#
#  # Pods that are common across platforms
#  pod 'GoogleAPIClientForREST/Drive', '~> 1.3.7'  # Ensure this supports macOS
#
#  # Pods for iOS-specific functionality
#  # Google Sign-In only works for iOS, so include it conditionally
#  pod 'GoogleSignIn', '~> 6.0', :target => 'DocumentManager' if !target.has_platform?(:macos)
#
#end
